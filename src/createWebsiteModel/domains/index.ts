import * as languages from "./languages";
import * as settings from "./settings";
import * as messageCatalog from "./messageCatalog";
import * as pages from "./pages/index";

export const collections = {
    languages,
    settings,
    messageCatalog,
    pages,
};
